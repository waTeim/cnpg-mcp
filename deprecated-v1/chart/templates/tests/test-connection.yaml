apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cnpg-mcp.fullname" . }}-test-connection"
  labels:
    {{- include "cnpg-mcp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cnpg-mcp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
